<h1>Events#index</h1>
<p>Find me in app/views/events/index.html.erb</p>






<%= month_calendar events: @events do |date, meetings| %>
<span class="my-date"><%= date.day %></span>
  <% meetings.each do |meeting| %>
    <div>
      <%= meeting.title %>
    </div>
  <% end %>
<% end %>









